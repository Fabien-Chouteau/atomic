name = "tests"
description = "Shiny new project"
version = "0.0.0"

executables = ["tests"]
[[depends-on]]
atomic = "*"
gnat_arm_elf = "*"

[[pins]]
atomic = { path='../..' }

[configuration.values]
atomic.backend = "armv6m"
